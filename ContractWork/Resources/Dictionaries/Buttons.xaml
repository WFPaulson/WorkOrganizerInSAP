<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="btnDefault" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="{TemplateBinding Border.CornerRadius}"
                        Background="{TemplateBinding Background}">
                        <Grid>
                            <ContentPresenter x:Name="MyContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="0,0,0,0" 
                                          />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>


    <Style x:Key="btnCircle" TargetType="Button" BasedOn="{StaticResource btnDefault}">
        <!--<Setter Property="Border.CornerRadius" Value="{TemplateBinding Border.CornerRadius}"/>-->
        <Setter Property="Height"
            Value="{Binding ActualWidth, RelativeSource={RelativeSource Self}}" />

    </Style>

    <Style x:Key="RoundBtnShadow" TargetType="Button" BasedOn="{StaticResource btnDefault}">
        <!--<Setter Property="Border.CornerRadius"  Value="45"/>-->
        <!--{Binding Border.CornerRadius, RelativeSource={RelativeSource Self}}"/>-->
        <!--Value="45"-->

        <Setter Property="Button.Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black"
                              Direction="320"
                              ShadowDepth="10"
                              BlurRadius="15"
                              Opacity="0.5" />
            </Setter.Value>
        </Setter>
    </Style>



</ResourceDictionary>