<Window x:Class="ContractWork.MVVM.ConvertToSAP.ConvertToSAPWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContractWork.MVVM.ConvertToSAP"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="ConvertToSAPWindow" Height="450" Width="800">
    <Border CornerRadius="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width=".6*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <DataGrid
                Grid.Row="1"
                Grid.Column="1"
                ItemsSource="{Binding SAPAccountNumbers}"
                SelectedItem="{Binding AccountSelected}"
                >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding OpenPopupForSAPUpdateCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                
            </DataGrid>
            <DataGrid
                Grid.Row="1"
                Grid.Column="3"
                ItemsSource="{Binding DBAccountNumbers}"
            >

            </DataGrid>

        </Grid>
        
    </Border>
</Window>
