<UserControl x:Class="ContractWork.CustomControls.WindowControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContractWork.CustomControls"
             xmlns:converters="clr-namespace:ContractWork.Converters"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Name="WindowControl">
    <UserControl.Resources>

        <!-- <converters:CornerRadiusRatioConverter x:Key="CornerRadiusRatio" /> -->
        <converters:DarkenColorConverter x:Key="Darken" />

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" 
                                   StrokeDashArray="1 2" 
                                   Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" 
                                   SnapsToDevicePixels="true" 
                                   StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--#region Color Section-->
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>

        <!--#endregion-->

        <Style x:Key="RoundedButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource btnCircle}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{Binding btnBckgrnd, ElementName=WindowControl}"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Border.CornerRadius" Value="{Binding brdrCornerRadius, ElementName=WindowControl}"/>
            <Setter Property="Foreground" Value="{Binding TextForeground, ElementName=WindowControl}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Margin" Value="{Binding btnMargin, ElementName=WindowControl}"/>
            <Setter Property="Padding" Value="{Binding TextPadding, ElementName=WindowControl}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="{TemplateBinding Border.CornerRadius}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" 
                                              Focusable="False" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              RecognizesAccessKey="True" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border"
                                        Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" 
                                        Value="{Binding btnBckgrnd, ElementName=WindowControl, 
                                                    Converter={StaticResource Darken}}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" 
                                        Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" 
                                        Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" 
                                        Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" 
                                        Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" 
                                        Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>

                            <!--{StaticResource Button.Disabled.Background}
                            {StaticResource Button.Disabled.Border}
                            {StaticResource Button.Disabled.Foreground}-->

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>

        <Button Style="{DynamicResource RoundedButtonStyle}" 
                Width="{Binding btnWidth, ElementName=WindowControl}"
                HorizontalAlignment="{Binding btnHorizAlign, ElementName=WindowControl}"
                HorizontalContentAlignment="{Binding btnContentHorizAlign, ElementName=WindowControl}"
                VerticalAlignment="{Binding btnVertAlign, ElementName=WindowControl}"
                VerticalContentAlignment="{Binding btnContentVertAlign, ElementName=WindowControl}"
                Command="{Binding btnCommand, ElementName=WindowControl}"
                CommandParameter="{Binding btnCmdParameter, ElementName=WindowControl}"
                Click="OnBtnClick"
                >

            <Button.Content>
                <StackPanel>
                    <Image Name="image1"  Source="{Binding ImageSource, ElementName=WindowControl}" Stretch="Fill" >
                        <Image.Style>
                            <Style TargetType="Image">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=ImageSource, 
                                        RelativeSource={RelativeSource Mode=FindAncestor, 
                                        AncestorType=Button}}" Value="null">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>

                    <TextBlock Margin="0"
                           FontSize="{Binding TextFontSize, ElementName=WindowControl}"
                           TextWrapping="Wrap"
                           FontFamily="Segoe UI"
                           FontWeight="Normal"
                           TextAlignment="Center"
                           Foreground="{Binding TextForeground, ElementName=WindowControl}"
                           VerticalAlignment="{Binding TextVertAlign, ElementName=WindowControl}"
                           Text="{Binding TextContent, ElementName=WindowControl}"
                            />
                </StackPanel>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
