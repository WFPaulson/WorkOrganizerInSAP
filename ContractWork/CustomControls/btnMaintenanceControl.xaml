<UserControl x:Class="ContractWork.CustomControls.btnMaintenanceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContractWork.CustomControls"
             xmlns:converters="clr-namespace:ContractWork.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="btnMaintenance">

    <UserControl.Resources>
        <converters:CornerRadiusRatioConverter x:Key="CornerRadiusRatio" />
        <converters:DarkenColorConverter x:Key="Darken" />

        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource RoundBtnShadow}">
            <Setter Property="Background"
                    Value="{Binding MaintBackground, ElementName=btnMaintenance}" />
            <Setter Property="Border.CornerRadius"  Value="{Binding MaintCornerRadius, ElementName=btnMaintenance}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background"
                            Value="{Binding MaintBackground, ElementName=btnMaintenance, Converter={StaticResource Darken}}" />
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" 
                                        Value="#A6AEB9"/>
                    <Setter Property="BorderBrush" 
                                        Value="{StaticResource Button.Pressed.Border}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--{StaticResource Button.Pressed.Background}
        {StaticResource Button.Pressed.Border}-->

    </UserControl.Resources>

    <Border 
        Background="{Binding btnBrdrBackground}" 
        Margin="{Binding btnBrdrMargin, ElementName=btnMaintenance}"
        Height="{Binding btnBrdrHeight, ElementName=btnMaintenance}"
        Width="{Binding btnBrdrWidth, ElementName=btnMaintenance}"
        BorderBrush="{Binding btnBrdrBrush, ElementName=btnMaintenance}"
        BorderThickness="{Binding btnBrdrThickness, ElementName=btnMaintenance}"
        CornerRadius="{Binding btnBrdrCornerRadius, ElementName=btnMaintenance}"
        HorizontalAlignment="{Binding btnHorizAlign, ElementName=btnMaintenance}"
        VerticalAlignment="{Binding btnVertAlign, ElementName=btnMaintenance}" 
        >
        <Border 
            BorderBrush="{Binding btn2ndBrdrBrush, ElementName=btnMaintenance}"
            BorderThickness="{Binding btn2ndBrdrThickness, ElementName=btnMaintenance}"
            CornerRadius="{Binding btnBrdrCornerRadius, ElementName=btnMaintenance}" 
            >

            <Grid>
                <Button 
                    Margin="0"
                    Height="{Binding btnHeight, ElementName=btnMaintenance}"
                    Width="{Binding btnWidth, ElementName=btnMaintenance}"
                    Command="{Binding btnClickCommand, ElementName=btnMaintenance}"
                    CommandParameter="{Binding btnCmdParameter, ElementName=btnMaintenance}"
                    Click="OnBtnClick"
                >
                <Button.Content>
                    <StackPanel>
                        <Image Name="image1"  
                               Source="{Binding ImageSource, ElementName=btnMaintenance}" 
                               Stretch="Fill" 
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Margin="{Binding ImageMargin, ElementName=btnMaintenance}">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=ImageSource, 
                                        RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}" 
                                                     Value="null">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>

                        <TextBlock 
                            FontSize="{Binding btnContentFontSize, ElementName=btnMaintenance}"
                            TextWrapping="Wrap"
                            FontFamily="Segoe UI"
                            FontWeight="Normal"
                            TextAlignment="Center"
                            Padding="{Binding textPadding, ElementName=btnMaintenance}"
                            Foreground="{Binding btnForeground, ElementName=btnMaintenance}"
                            VerticalAlignment="{Binding TextVertAlign, ElementName=btnMaintenance}"
                            Text="{Binding btnContentText, ElementName=btnMaintenance}"
                        />
                    </StackPanel>
                </Button.Content>

            </Button>
            
        </Grid>
        </Border>
    </Border>
</UserControl>
